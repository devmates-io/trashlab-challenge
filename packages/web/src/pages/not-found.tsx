import { StubPage } from "./_stub";

export default function NotFoundPage() {
  return (
    <StubPage
      title="Not found"
      detail="We couldn't find that page. Use the sidebar to navigate."
    />
  );
}
